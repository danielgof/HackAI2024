name: compas
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  camera: ^0.10.5+9
  path_provider: ^2.1.2
  path: ^1.9.0
  dart_openai: ^5.0.0
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/land_back.png
    - assets/brutus_buckeye.jpeg
    # - assets/offers/